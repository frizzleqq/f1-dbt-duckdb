version: 2

models:
  - name: drivers
    columns:
      - name: driver_id
        tests:
          - unique
          - not_null
  - name: circuits
    columns:
      - name: circuit_id
        tests:
          - unique
          - not_null
