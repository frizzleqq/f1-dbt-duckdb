version: 2

models:
  - name: drivers
    columns:
      - name: id
        tests:
          - unique
          - not_null
  - name: circuits
    columns:
      - name: id
        tests:
          - unique
          - not_null
