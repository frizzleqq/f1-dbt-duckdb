# there are two ways to set storage to SqliteStorage

# this config sets the directory (`base_dir`) for Sqlite to store data in:
storage:
  sqlite:
    base_dir:
      env: DATA_DIR

telemetry:
  enabled: false

run_queue:
  max_concurrent_runs: 4
